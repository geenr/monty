#include "monty.h"
/**
 * queue_monty - prints the top
 * @head: stack head
 * @line_no: line_number
 * Return: no return
*/
void queue_monty(stack_t **head, unsigned int line_no)
{
	(void)head;
	(void)line_no;
	bus.lifi = 1;
}
