#include "monty.h"

/**
 * my_nop - doesn't do anything
 * @head: head stack of the linked list
 * @line_no: line number in bytecode
 * Return: No return
 */
void my_nop(stack_t **head, unsigned int line_no)
{
	(void) head;
	(void) line_no;
}
